import { describe, it, expect } from 'vitest'

describe('Prueba de Vida del Entorno', () => {
  
  it('Las matemáticas básicas deberían funcionar', () => {
    const suma = 2 + 2;
    expect(suma).toBe(4);
  })

})